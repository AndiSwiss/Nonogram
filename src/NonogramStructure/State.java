package NonogramStructure;

public enum State {
    UNKNOWN,
    WHITE,
    BLACK

    // add other color if necessary!
    // for each state, the UNKNOWN and WHITE has to be also a state, e.g.:
    // RED, RED_NOT, RED_UNKNOWN
    // BLUE, BLUE_NOT, BLUE_UNKNOWN
}
