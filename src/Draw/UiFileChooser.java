package Draw;

import Data.Position;

public class UiFileChooser extends UiElement {
    public UiFileChooser(String name, String message, Position start, int relSizeX, int relSizeY) {
        super(name, message, start, relSizeX, relSizeY);
    }

}
