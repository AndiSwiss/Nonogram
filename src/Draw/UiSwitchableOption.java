package Draw;

import Data.Position;

public class UiSwitchableOption extends UiElement {
    public UiSwitchableOption(String name, String message, Position start, int relSizeX, int relSizeY) {
        super(name, message, start, relSizeX, relSizeY);
    }
}
