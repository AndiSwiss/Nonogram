package Solvers;

public class Solver4 {

    /* todo:
     *  - nonogram5 can not get solved with strategies 1 - 3
     *      -> find new solution-strategies!
     *  - write a checker, which would check for errors comparing to the numbers
     *  - write a checker, which would check for errors comparing to an already existing solution, if there is one
     *  - create a new UI-Zone for error-output (in red)
     *  - create a total solver for a nonogram, which runs one operation every half second or so and stops, if it
     *      cannot solve anything more
     *  - write a method, which crosses out already solved numbers
     */

}
